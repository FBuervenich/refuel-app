{
  "name": "common",
  "version": "0.1.0",
  "description": "Common code, shared by multiple packages in the repo",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "dependencies": {},
  "devDependencies": {}
}
